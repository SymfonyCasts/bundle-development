<!DOCTYPE html>
<html>
    <head>
        <meta charset="UTF-8">
        <title>{% block title %}Welcome to the SpaceBar{% endblock %}</title>
        <link rel="icon" href="data:image/svg+xml,<svg xmlns=%22http://www.w3.org/2000/svg%22 viewBox=%220 0 128 128%22><text y=%221.2em%22 font-size=%2296%22>⚫️</text><text y=%221.3em%22 x=%220.2em%22 font-size=%2276%22 fill=%22%23fff%22>sf</text></svg>">
        {% block stylesheets %}
        {% endblock %}

        {% block javascripts %}
            {% block importmap %}{{ importmap('app') }}{% endblock %}
        {% endblock %}
    </head>
    <body class="bg-[url('../images/bg-planet.png')] bg-contain bg-no-repeat">
        <nav class="flex items-center justify-between flex-wrap py-6 mx-auto px-4">
            <div class="flex items-end flex-shrink-0 mr-6 text-white">
                <a href="{{ path('app_homepage') }}" class="font-semibold text-xl tracking-tight">
                    <img class="max-h-[45px]" src="{{ asset('images/spacebar-logo.png') }}" alt="Spacebar Logo">
                </a>
                <a href="#" class="block ml-6 mt-4 lg:inline-block lg:mt-0 hover:text-[#5BA8DC] mr-6">Local Asteroids</a>
                <a href="#" class="block ml-6 mt-4 lg:inline-block lg:mt-0 hover:text-[#5BA8DC] mr-6">Weather</a>
                <a href="#" class="block ml-6 mt-4 lg:inline-block lg:mt-0 hover:text-[#5BA8DC] mr-6">Journal</a>
            </div>
            <div class="flex">
                <form class="max-w-md mx-auto mr-1.5">   
                    <label for="default-search" class="mb-2 text-sm font-medium text-gray-900 sr-only dark:text-white">Search</label>
                    <div class="relative">
                        <div class="absolute inset-y-0 start-0 flex items-center ps-3 pointer-events-none">
                            <svg class="w-4 h-4 text-gray-900" aria-hidden="true" xmlns="http://www.w3.org/2000/svg" fill="none" viewBox="0 0 20 20">
                                <path stroke="currentColor" stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="m19 19-4-4m0-7A7 7 0 1 1 1 8a7 7 0 0 1 14 0Z"/>
                            </svg>
                        </div>
                        <input type="search" id="default-search" class="block w-full p-4 ps-10 text-sm text-gray-900 rounded-[40px] bg-white focus:ring-blue-500 focus:border-blue-500 " placeholder="Search" required />
                    </div>
                </form>
                <div class="block lg:hidden">
                    <button class="flex items-center px-3 py-2">
                        <img class="h-14 w-14" src="{{ asset('images/profile-img.png') }}">
                    </button>
                </div>
                <div class="hidden w-full flex-grow lg:flex lg:items-center lg:w-auto">
                    <div class="font-bold uppercase lg:flex-grow">
                        <img class="h-14 w-14" src="{{ asset('images/profile-img.png') }}">
                    </div>
                </div>
            </div>
        </nav>

        {% block body %}{% endblock %}

        <footer class="footer">
            <div class="container text-center">
                <span class="text-muted">Made with <i class="fa fa-heart" style="color: red;"></i> by the guys and gals at <a href="https://symfonycasts.com">SymfonyCasts</a></span>
            </div>
        </footer>
    </body>
</html>
