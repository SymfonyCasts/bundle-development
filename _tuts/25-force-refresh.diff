diff --git a/object-translation-bundle/src/ObjectTranslator.php b/object-translation-bundle/src/ObjectTranslator.php
index d69f63b..707d6e0 100644
--- a/object-translation-bundle/src/ObjectTranslator.php
+++ b/object-translation-bundle/src/ObjectTranslator.php
@@ -31,7 +31,7 @@ final class ObjectTranslator
      *
      * @return T
      */
-    public function translate(object $object, ?string $locale = null): object
+    public function translate(object $object, ?string $locale = null, array $options = []): object
     {
         $locale = $locale ?? $this->localeAware->getLocale();
 
@@ -39,10 +39,14 @@ final class ObjectTranslator
             return $object;
         }
 
-        return $this->translatedObjects[$object] ??= new TranslatedObject($object, $this->translationsFor($object, $locale));
+        return $this->translatedObjects[$object] ??= new TranslatedObject($object, $this->translationsFor(
+            $object,
+            $locale,
+            $options['force_refresh'] ?? false,
+        ));
     }
 
-    private function translationsFor(object $object, string $locale): array
+    private function translationsFor(object $object, string $locale, bool $forceRefresh): array
     {
         $type = $this->mappingManager->translatableTypeFor($object);
         $id = $this->mappingManager->idFor($object);
@@ -59,7 +63,8 @@ final class ObjectTranslator
                 }
 
                 return $this->mappingManager->translationsFor($locale, $type, $id);
-            }
+            },
+            $forceRefresh ? \INF : null,
         );
     }
 }
